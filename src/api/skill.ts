// export const magicWarriorMasteryLevel =
//   (level: number) =>
//   <S>(status: S) => ({ ...status, magicWarriorMasteryLevel: level });

//   // calc

//   export const magicWarriorMastery = <S extends {totalFlatCSPD: number, totalPercentCSPD: number}>(status: S) => {

//     return {...status, totalFlatCSPD: status.totalFlatCSPD + ()}
//   }
